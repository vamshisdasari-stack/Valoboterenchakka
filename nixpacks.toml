# nixpacks.toml
# Tells Railway/Render to install system libraries needed by node-canvas

[phases.setup]
nixPkgs = [
  "cairo",
  "pango",
  "libjpeg",
  "giflib",
  "librsvg",
  "pixman",
  "pkg-config"
]
